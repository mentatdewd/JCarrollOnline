<% if @feed_items.any? %>
  <ol id="feed_items" class="feed_items">
     <%= render partial: 'shared/feed_item', collection: @feed_items %>
  </ol>
  <%= will_paginate @feed_items, :inner_window => 2, :outer_window => 1 %>
<% end %>